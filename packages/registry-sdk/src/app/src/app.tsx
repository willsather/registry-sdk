import * as React from "react";

export function App() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center gap-2 bg-gray-400 text-center">
      <h1 className="font-extrabold font-mono text-primary">Registry</h1>
      <p className="italic">
        this is a registry running using `registry-sdk` and styled by tailwind
      </p>
    </div>
  );
}
